version: "3.8"

services:
  app:
    build: .
    ports:
      - "6969:6969"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - FRONTEND_URL=https://adgen.sigmagrowthpartners.com
    volumes:
      - app_data:/data
    networks:
      - shared_n8n

networks:
  shared_n8n:
    external: true

volumes:
  app_data:
